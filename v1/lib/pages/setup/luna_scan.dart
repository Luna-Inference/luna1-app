import 'package:flutter/material.dart';

class LunaScanPage extends StatelessWidget {
  const LunaScanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
